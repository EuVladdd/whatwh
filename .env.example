# Optional. Copy to .env. Never commit .env or the data folder.
PORT=3000
HOST=127.0.0.1
# Set a unique password of at least 12 characters, or leave empty for automatic setup.
ADMIN_PASSWORD=
# Set true when the public site is served through HTTPS.
COOKIE_SECURE=false
